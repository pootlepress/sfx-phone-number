/**
 * Created by shramee on 28/4/15.
 */
( function( $ ) {
    $( document).ready( function(){
/*
        $( '.sfxpx-phone').each( function(){

            var $t = $( this );

            var $a = $t.find( 'a' );

            var href = $a.attr('href').replace( 'http://', 'callto://' );

            $a.attr( 'href', href );

            var classes = $.grep($t.attr( 'class' ).split(" "), function(v, i){
                return v.indexOf('fa') === 0;
            }).join( ' ' );

            $a.attr( 'class', classes );

        } )
*/
    } );





} )( jQuery );
